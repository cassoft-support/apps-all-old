# Структура директорий app.cassoft.ru

> **Статус:** 📋 Требуется заполнение после анализа проекта

## Обзор

**Путь на сервере:** `/var/www/bitirx-brokci/data/www/app.cassoft.ru`

---

## Корневая структура

```
app.cassoft.ru/
├── .github/                    - документация проекта
│   ├── copilot-instructions.md - главные инструкции
│   └── instructions/           - специфичные инструкции
├── .gitignore                  - исключения Git
├── bitrix/                     - ядро Битрикс24 (не в Git!)
├── local/                      - локальные компоненты и классы
├── upload/                     - загруженные файлы (не в Git!)
├── index.php                   - точка входа приложения
└── .settings.php               - конфигурация БД (не в Git!)
```

---

## Директория /local/

Основная директория для кастомных разработок.

```
local/
├── components/                 - кастомные компоненты
│   └── (требуется анализ)
├── classes/                    - PHP классы
│   └── (требуется анализ)
├── functions/                  - вспомогательные функции
│   └── (требуется анализ)
├── php_interface/              - события и хуки
│   └── (требуется анализ)
└── templates/                  - шаблоны
    └── (требуется анализ)
```

---

## Структура компонента

Типичная структура компонента Битрикс24:

```
/local/components/[vendor]/[component_name]/
├── component.php               - основная логика компонента
├── .description.php            - описание компонента
├── .parameters.php             - параметры компонента
├── lang/                       - языковые файлы
│   └── ru/
│       └── .description.php
├── templates/                  - шаблоны компонента
│   ├── .default/
│   │   ├── template.php        - HTML шаблон
│   │   ├── style.css           - стили
│   │   ├── script.js           - JavaScript
│   │   └── lang/
│   └── [custom_template]/
└── ajax/                       - AJAX обработчики (если есть)
    ├── ajax.php
    ├── save.php
    └── save_function.php
```

---

## Компоненты проекта

**Требуется:** Анализ структуры `/local/components/`

### Поиск компонентов
```bash
# На сервере
cd /var/www/bitirx-brokci/data/www/app.cassoft.ru/local/components
find . -type d -maxdepth 2
```

### Список компонентов

| Vendor | Component | Назначение | Шаблоны |
|--------|-----------|------------|---------|
| ? | ? | ? | ? |

---

## Директория /upload/

Загруженные пользователями файлы (не версионируются в Git).

```
upload/
├── (структура зависит от приложения)
└── (требуется анализ)
```

**Важно:** Директория `/upload/` исключена из Git через `.gitignore`

---

## Директория /bitrix/

Ядро Битрикс24 (не версионируется в Git).

```
bitrix/
├── modules/                    - модули системы
├── components/                 - системные компоненты
├── js/                         - JavaScript системы
├── css/                        - CSS системы
└── (другие системные файлы)
```

**Важно:** Директория `/bitrix/` исключена из Git через `.gitignore`

---

## Конфигурационные файлы

### .gitignore
Определяет файлы, исключенные из версионирования:
- `/bitrix/` - ядро системы
- `/upload/` - загруженные файлы
- `.settings.php` - конфигурация БД (пароли!)
- `*.log` - лог-файлы
- `/pub/` - временные публичные файлы

### .settings.php
Конфигурация подключения к БД и другие настройки (не в Git!).

### composer.json (если есть)
Зависимости PHP проекта.

### package.json (если есть)
Зависимости JavaScript проекта.

---

## Логи и временные файлы

### Логи PHP
- **Системные:** `/var/log/php8.2-fpm/error.log`
- **Приложение:** (требуется анализ)

### Временные файлы
- `/pub/` - публичные временные файлы (не в Git)
- `/tmp/` - временные файлы (если есть)

---

## Важные файлы проекта

**Требуется:** Поиск ключевых файлов

### Поиск PHP файлов
```bash
# На сервере
cd /var/www/bitirx-brokci/data/www/app.cassoft.ru
find ./local -name "*.php" -type f | head -20
```

### Поиск JavaScript файлов
```bash
# На сервере
find ./local -name "*.js" -type f | head -20
```

### Таблица ключевых файлов

| Файл | Путь | Назначение |
|------|------|------------|
| ? | ? | ? |

---

## Размер проекта

**Требуется:** Анализ размеров директорий

```bash
# На сервере
du -sh /var/www/bitirx-brokci/data/www/app.cassoft.ru/*
```

### Статистика
- **Общий размер:** (требуется)
- **Размер /local/:** (требуется)
- **Размер /upload/:** (требуется)
- **Размер /bitrix/:** (требуется)
- **Количество PHP файлов:** (требуется)
- **Количество JS файлов:** (требуется)

---

## Рекомендации по структуре

### Что должно быть в Git
- ✅ `/local/` - все кастомные разработки
- ✅ `/.github/` - документация проекта
- ✅ `.gitignore` - правила исключений
- ✅ `index.php` - если модифицирован
- ✅ `composer.json`, `package.json` - зависимости

### Что НЕ должно быть в Git
- ❌ `/bitrix/` - ядро системы
- ❌ `/upload/` - пользовательские файлы
- ❌ `.settings.php` - конфигурация с паролями
- ❌ `*.log` - лог-файлы
- ❌ `/pub/` - временные файлы
- ❌ Большие файлы (>50MB)

---

## Ссылки

- [Git Workflow](git-workflow.instructions.md) - работа с Git
- [PHP Backend](php-backend.instructions.md) - стандарты PHP
- [JavaScript](javascript.instructions.md) - стандарты JS

---

**Документ создан:** 11 ноября 2025 г.  
**Статус:** Требуется заполнение  
**Следующий шаг:** Анализ структуры `/local/components/` на сервере
